import datas from '../datas/logements.json'

let index = [datas[1], datas[2], datas[3], datas[4], datas[5],datas[6]]
let indexes = []
// while (indexes.length < 6){
//     const randomIndex = Math.floor(Math.random() * (datas.length - 1))
//     // indexes[datas(randomIndex).id] < -1 ? indexes.push(datas(randomIndex)) : null
//     console.log(randomIndex)
// }

const ThumbsArray = index

export default ThumbsArray